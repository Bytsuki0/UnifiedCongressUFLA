# Netlify / Cloudflare Pages — reescrita de SPA.
#
# Toda URL que não corresponda a um arquivo real serve o index.html com
# status 200 (não 301/302: a URL precisa continuar a mesma para o React
# Router resolvê-la). Sem esta regra, recarregar a página em
# /estudante/historico devolve 404.
#
# Arquivos existentes (/assets/*, /robots.txt, /favicon.svg) têm
# precedência sobre esta regra e continuam sendo servidos normalmente.

/*    /index.html   200
