# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
vite_project
# Claude Code: só o settings.json do projeto (allowlist compartilhada) é versionado;
# settings.local.json e o resto de .claude/ ficam locais.
.claude/*
!.claude/settings.json
# Instruções locais do Claude — não versionar (pedido do time)
CLAUDE.md
# Documentação interna de segurança — não versionar
SECURITY_ANALYSIS.md
SECURITY_SETUP.md
.config
./vite_project
vite_project/*
!.gitignore
docs/*
.docs
docs
